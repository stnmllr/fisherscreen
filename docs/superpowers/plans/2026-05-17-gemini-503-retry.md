# Gemini 503-Retry Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transiente Gemini-Fehler (503 UNAVAILABLE, 429 RESOURCE_EXHAUSTED) werden mit exponentiellem Backoff (1s/4s/16s, max 4 Calls) retried, statt den Ticker für 30 Tage zu verlieren.

**Architecture:** Retry-Decorator (`tenacity`) auf zwei aus `score_ticker()` extrahierten Private-Methoden `_count_tokens()` / `_generate()` in `GeminiClientImpl`. `reraise=True` → originale genai-Exception fliegt nach erschöpften Retries und wird vom bestehenden `except Exception → GeminiError`-Block gewrappt. `CachedGeminiClient` und `scorer.py` unverändert.

**Tech Stack:** Python 3.12, tenacity 9.1.4, google-genai, pytest, uv. Shell: cmd.exe. Tests: `uv run python -m pytest` (SOPRA-Workaround — nie `uv run pytest`).

**Spec:** `docs/superpowers/specs/2026-05-17-gemini-503-retry-design.md`

> **Coverage-Hinweis (wichtig für den Executor):** `pyproject.toml` setzt `[tool.coverage.run] omit = ["app/services/*"]`. `app/services/gemini_client.py` ist damit **nicht** in der Coverage-Messung. Der Gate ist `--cov-fail-under=90`. Die ~95,39 % ändern sich durch diese Aufgabe **nicht** — das ist erwartet, kein Fehler. Tests werden für Korrektheit geschrieben, nicht für die Coverage-Zahl. Keine Coverage-Gewinn-Behauptung aufstellen.

---

### Task 0: Tenacity-Verhalten verifizieren (Hard Gate)

**Files:** keine (reine Verifikation)

- [ ] **Step 1: Tenacity wait_exponential gegen 1/4/16 prüfen**

Run (cmd.exe, im Repo-Root):
```
.venv\Scripts\python.exe -c "from importlib.metadata import version; print('tenacity', version('tenacity')); from tenacity import wait_exponential; w=wait_exponential(multiplier=1, exp_base=4); RS=type('RS',(),{}); [print('after attempt', n, '->', w(type('R',(),{'attempt_number':n})())) for n in (1,2,3)]"
```

Erwartete Ausgabe (exakt):
```
tenacity 9.1.4
after attempt 1 -> 1
after attempt 2 -> 4
after attempt 3 -> 16
```

- [ ] **Step 2: Gate**

Wenn die Ausgabe **abweicht** (anderes Schema als 1/4/16, andere Major-Version, `exp_base` nicht akzeptiert): **STOPPEN, nicht improvisieren.** Ausgabe an Stephan zeigen, gemeinsam entscheiden ob `wait_chain(wait_fixed(1), wait_fixed(4), wait_fixed(16))` als Fallback nötig ist. **Nicht** eigenmächtig auf `wait_chain` wechseln.

Wenn die Ausgabe stimmt: weiter mit Task 1. (Bereits am 2026-05-17 vorab durch Claude verifiziert — Schema bestätigt.)

---

### Task 1: tenacity als direkte Dependency

**Files:**
- Modify: `pyproject.toml` (dependencies-Liste)
- Modify: `uv.lock` (automatisch durch uv)

- [ ] **Step 1: Dependency hinzufügen**

Run (cmd.exe):
```
uv add tenacity
```
`tenacity 9.1.4` ist bereits transitiv in `uv.lock` — dieser Schritt promoviert sie zur direkten Dependency in `pyproject.toml`. Kein neuer Download erwartet. CLAUDE.md-konform: bewusster Schritt, hier dokumentiert.

- [ ] **Step 2: Verifizieren**

Run:
```
uv run python -c "import tenacity; from importlib.metadata import version; print(version('tenacity'))"
```
Expected: `9.1.4` (oder kompatible 9.x — falls Major-Version ≠ 9: STOPPEN, Task-0-Gate erneut anwenden).

Prüfen dass `pyproject.toml` jetzt `"tenacity>=9.1.4"` (oder von uv gesetzte Spanne) in `[project] dependencies` enthält.

- [ ] **Step 3: Commit**

```
git add pyproject.toml uv.lock
git commit -m "chore: add tenacity as direct dependency for Gemini retry"
```

---

### Task 2: Retry-Unit-Tests (TDD — rot zuerst)

**Files:**
- Modify: `tests/services/test_gemini_client.py` (Tests + Fixture + Imports ergänzen)

- [ ] **Step 1: Failing Tests + Fixture schreiben**

Am Anfang von `tests/services/test_gemini_client.py` die Imports ergänzen:

```python
from google.genai.errors import ClientError, ServerError
```

Helper für genai-Fehler und Fixture (nach den bestehenden Helpern, vor dem ersten Test einfügen):

```python
def _server_error(code: int) -> ServerError:
    return ServerError(code, {"error": {"status": "UNAVAILABLE", "message": "high demand"}})


def _client_error(code: int) -> ClientError:
    return ClientError(code, {"error": {"status": "RESOURCE_EXHAUSTED", "message": "rate"}})


@pytest.fixture
def fast_retry(monkeypatch):
    """Neutralisiert tenacity-Sleep, damit Retry-Tests nicht real warten."""
    monkeypatch.setattr(GeminiClientImpl._count_tokens.retry, "sleep", lambda _: None)
    monkeypatch.setattr(GeminiClientImpl._generate.retry, "sleep", lambda _: None)
```

Folgende Tests ans Ende der Datei anhängen:

```python
@patch("app.services.gemini_client._genai")
def test_retries_generate_on_503_then_succeeds(mock_genai, fast_retry):
    mock_client = MagicMock()
    mock_genai.Client.return_value = mock_client
    mock_client.models.count_tokens.return_value = _mock_token_resp(500)
    mock_client.models.generate_content.side_effect = [
        _server_error(503),
        _server_error(503),
        _mock_generate_resp(_valid_dims()),
    ]

    impl = GeminiClientImpl(api_key="key")
    result = impl.score_ticker("TEST", _record())

    assert result.dimensions == _valid_dims()
    assert mock_client.models.generate_content.call_count == 3


@patch("app.services.gemini_client._genai")
def test_retries_count_tokens_on_503_then_succeeds(mock_genai, fast_retry):
    mock_client = MagicMock()
    mock_genai.Client.return_value = mock_client
    mock_client.models.count_tokens.side_effect = [
        _server_error(503),
        _mock_token_resp(500),
    ]
    mock_client.models.generate_content.return_value = _mock_generate_resp(_valid_dims())

    impl = GeminiClientImpl(api_key="key")
    result = impl.score_ticker("TEST", _record())

    assert result.dimensions == _valid_dims()
    assert mock_client.models.count_tokens.call_count == 2


@patch("app.services.gemini_client._genai")
def test_persistent_503_raises_gemini_error_after_4_calls(mock_genai, fast_retry):
    mock_client = MagicMock()
    mock_genai.Client.return_value = mock_client
    mock_client.models.count_tokens.return_value = _mock_token_resp(500)
    mock_client.models.generate_content.side_effect = [_server_error(503)] * 4

    impl = GeminiClientImpl(api_key="key")
    with pytest.raises(GeminiError, match="API call failed"):
        impl.score_ticker("TEST", _record())
    assert mock_client.models.generate_content.call_count == 4


@patch("app.services.gemini_client._genai")
def test_retries_on_429(mock_genai, fast_retry):
    mock_client = MagicMock()
    mock_genai.Client.return_value = mock_client
    mock_client.models.count_tokens.return_value = _mock_token_resp(500)
    mock_client.models.generate_content.side_effect = [
        _client_error(429),
        _mock_generate_resp(_valid_dims()),
    ]

    impl = GeminiClientImpl(api_key="key")
    result = impl.score_ticker("TEST", _record())

    assert result.dimensions == _valid_dims()
    assert mock_client.models.generate_content.call_count == 2


@patch("app.services.gemini_client._genai")
def test_no_retry_on_client_error_400(mock_genai, fast_retry):
    mock_client = MagicMock()
    mock_genai.Client.return_value = mock_client
    mock_client.models.count_tokens.return_value = _mock_token_resp(500)
    mock_client.models.generate_content.side_effect = _client_error(400)

    impl = GeminiClientImpl(api_key="key")
    with pytest.raises(GeminiError, match="API call failed"):
        impl.score_ticker("TEST", _record())
    assert mock_client.models.generate_content.call_count == 1


@patch("app.services.gemini_client._genai")
def test_no_retry_on_server_error_500(mock_genai, fast_retry):
    mock_client = MagicMock()
    mock_genai.Client.return_value = mock_client
    mock_client.models.count_tokens.return_value = _mock_token_resp(500)
    mock_client.models.generate_content.side_effect = _server_error(500)

    impl = GeminiClientImpl(api_key="key")
    with pytest.raises(GeminiError, match="API call failed"):
        impl.score_ticker("TEST", _record())
    assert mock_client.models.generate_content.call_count == 1
```

- [ ] **Step 2: Tests laufen lassen, Rot bestätigen**

Run:
```
uv run python -m pytest tests/services/test_gemini_client.py -v
```
Expected: Die 6 neuen Tests **FAIL** (u.a. `AttributeError: ... has no attribute '_count_tokens'` in der Fixture / `_generate`), die 7 bestehenden Tests bleiben grün.

---

### Task 3: Retry implementieren (grün machen)

**Files:**
- Modify: `app/services/gemini_client.py`

- [ ] **Step 1: Imports + Retry-Prädikat ergänzen**

In `app/services/gemini_client.py` die Imports erweitern (nach `from google.genai import types as _types`):

```python
from google.genai.errors import ClientError, ServerError
from tenacity import retry, retry_if_exception, stop_after_attempt, wait_exponential
```

Modul-Level-Funktion direkt nach `logger = logging.getLogger(__name__)` / `_DEFAULT_MODEL`-Block einfügen:

```python
def _is_retryable(exc: BaseException) -> bool:
    return (isinstance(exc, ServerError) and exc.code == 503) or (
        isinstance(exc, ClientError) and exc.code == 429
    )


_RETRY = dict(
    retry=retry_if_exception(_is_retryable),
    stop=stop_after_attempt(4),
    wait=wait_exponential(multiplier=1, exp_base=4),
    reraise=True,
)
```

- [ ] **Step 2: `score_ticker` refaktorieren + dekorierte Methoden hinzufügen**

`GeminiClientImpl.score_ticker` so ersetzen, dass die rohen Client-Calls in zwei dekorierte Private-Methoden ausgelagert sind. Die bestehenden `try/except → GeminiError`-Blöcke bleiben in `score_ticker` und wrappen die nach `reraise=True` durchgereichte Original-Exception:

```python
    def score_ticker(
        self,
        ticker: str,
        record: ScreenerRecord,
        max_input_tokens: int = 3000,
        max_output_tokens: int = 1000,
    ) -> GeminiScoreResult:
        prompt = _build_prompt(ticker, record)
        try:
            token_resp = self._count_tokens(prompt)
        except Exception as exc:
            logger.warning("ticker=%s token count failed: %s", ticker, exc)
            raise GeminiError(f"Token count failed for {ticker}: {exc}") from exc
        if token_resp.total_tokens > max_input_tokens:
            logger.warning(
                "ticker=%s prompt too large: %d > %d tokens — skipping",
                ticker, token_resp.total_tokens, max_input_tokens,
            )
            raise GeminiError(
                f"ticker={ticker} prompt too large: {token_resp.total_tokens} > {max_input_tokens} tokens"
            )
        try:
            response = self._generate(prompt, max_output_tokens)
        except Exception as exc:
            logger.warning("ticker=%s Gemini API call failed: %s", ticker, exc)
            raise GeminiError(f"Gemini API call failed for {ticker}: {exc}") from exc
        return _parse_response(ticker, response)

    @retry(**_RETRY)
    def _count_tokens(self, prompt: str) -> Any:
        return self._client.models.count_tokens(model=self._model, contents=prompt)

    @retry(**_RETRY)
    def _generate(self, prompt: str, max_output_tokens: int) -> Any:
        return self._client.models.generate_content(
            model=self._model,
            contents=prompt,
            config=_types.GenerateContentConfig(
                response_mime_type="application/json",
                max_output_tokens=max_output_tokens,
            ),
        )
```

- [ ] **Step 3: Tests laufen lassen, Grün bestätigen**

Run:
```
uv run python -m pytest tests/services/test_gemini_client.py -v
```
Expected: **alle** Tests PASS (7 bestehende unverändert grün + 6 neue grün). Die bestehenden Tests bestätigen, dass das Refactoring verhaltensneutral ist (kein retrybarer Fehler → genau 1 Call wie zuvor).

- [ ] **Step 4: Commit**

```
git add app/services/gemini_client.py tests/services/test_gemini_client.py
git commit -m "feat: retry Gemini 503/429 with exponential backoff"
```

---

### Task 4: Integrationstest gegen CachedGeminiClient

**Files:**
- Modify: `tests/services/test_cached_gemini_client.py`

- [ ] **Step 1: Failing Integrationstest schreiben**

Imports am Dateianfang ergänzen:

```python
from unittest.mock import MagicMock, patch

from app.services.gemini_client import GeminiClientImpl
from google.genai.errors import ServerError
```

(Falls `from unittest.mock import MagicMock` schon vorhanden ist, nur `patch` ergänzen und Doppel-Import vermeiden.)

Test ans Ende anhängen:

```python
@patch("app.services.gemini_client._genai")
def test_retry_survives_cache_layer_on_503(mock_genai):
    """503→Erfolg im echten GeminiClientImpl, durch CachedGeminiClient hindurch."""
    GeminiClientImpl._generate.retry.sleep = lambda _: None  # kein reales Warten

    mock_inner = MagicMock()
    mock_genai.Client.return_value = mock_inner
    token_resp = MagicMock()
    token_resp.total_tokens = 500
    mock_inner.models.count_tokens.return_value = token_resp
    good = MagicMock()
    good.text = (
        '{"dimensions": {"growth": 4, "profitability": 3, "management": 4, '
        '"innovation": 5, "resilience": 3}, "summary": "ok"}'
    )
    good.usage_metadata.prompt_token_count = 500
    good.usage_metadata.candidates_token_count = 80
    mock_inner.models.generate_content.side_effect = [
        ServerError(503, {"error": {"message": "high demand"}}),
        good,
    ]

    impl = GeminiClientImpl(api_key="key")
    mock_fs = MagicMock()
    mock_fs.get.return_value = None  # Cache-Miss

    client = CachedGeminiClient(gemini=impl, firestore=mock_fs, collection="col")
    result = client.score_ticker("AAPL", _record())

    assert result.dimensions["growth"] == 4
    assert mock_inner.models.generate_content.call_count == 2
    mock_fs.set.assert_called_once()
```

- [ ] **Step 2: Test laufen lassen**

Run:
```
uv run python -m pytest tests/services/test_cached_gemini_client.py::test_retry_survives_cache_layer_on_503 -v
```
Expected: PASS (Implementierung aus Task 3 ist bereits vorhanden — dieser Test verifiziert das Zusammenspiel, kein neuer Produktiv-Code nötig).

- [ ] **Step 3: Commit**

```
git add tests/services/test_cached_gemini_client.py
git commit -m "test: verify Gemini retry survives CachedGeminiClient layer"
```

---

### Task 5: Volle Suite, Spec-Abgleich, PR-Vorbereitung

**Files:**
- Modify: `docs/superpowers/specs/2026-05-17-gemini-503-retry-design.md` (Akzeptanz-Checkboxen + Coverage-Wording)
- Modify: `Projektstand.md` (TODO #11 als erledigt markieren)

- [ ] **Step 1: Volle Test-Suite grün**

Run:
```
uv run python -m pytest
```
Expected: alle Tests PASS (vorher 240 → jetzt 247: 6 Retry-Unit + 1 Integration). `--cov-fail-under=90` erfüllt. Coverage-Zahl ~95,39 % **unverändert** (Services sind ge-`omit`-tet — erwartet, siehe Coverage-Hinweis oben). Keine Coverage-Gewinn-Behauptung.

- [ ] **Step 2: Spec-Akzeptanzkriterien abhaken + Coverage-Wording korrigieren**

In `docs/superpowers/specs/2026-05-17-gemini-503-retry-design.md` die Checkboxen auf `[x]` setzen. Das Kriterium `- [ ] Coverage ≥ 95%` ersetzen durch:
```
- [x] `--cov-fail-under=90` erfüllt; Coverage-Zahl unverändert ~95,39 % (gemini_client.py ist via `omit = ["app/services/*"]` nicht gemessen — Tests dienen Korrektheit, nicht der Zahl)
```

- [ ] **Step 3: Projektstand.md — TODO #11 erledigt**

In `Projektstand.md` TODO #11 auf erledigt setzen (analog Format von TODO #7: `~~...~~ ✅ (2026-05-17)`), Kurzbegründung: tenacity-Retry für 503/429, beide Gemini-Calls abgesichert, 1s/4s/16s, max 4 Calls, 247 Tests grün. Branch/Commit referenzieren.

- [ ] **Step 4: Commit**

```
git add docs/superpowers/specs/2026-05-17-gemini-503-retry-design.md Projektstand.md
git commit -m "docs: mark Gemini 503-retry (TODO #11) done"
```

- [ ] **Step 5: PR vorbereiten**

Run:
```
git push -u origin feature/gemini-503-retry
```
Dann PR via `gh pr create` gegen `main`. Titel: `feat: retry Gemini 503/429 with exponential backoff (TODO #11)`. Body: Problem (ALV.DE-Verlust durch transientes 503), Lösung (tenacity, beide Calls, 1s/4s/16s, max 4), Test-Abdeckung (6 Unit + 1 Integration), Coverage-Hinweis (Services ge-omit-tet — Zahl unverändert, erwartet). Vor dem `gh pr create` an Stephan zur Freigabe — kein Merge auf `main` ohne explizite Freigabe (CLAUDE.md).

---

## Self-Review

**Spec-Coverage:**
- Retry-Prädikat 503+429 → Task 3 Step 1 ✓
- Beide Calls (`_count_tokens` + `_generate`) → Task 3 Step 2; Tests Task 2 (`test_retries_count_tokens_on_503...`) ✓
- `reraise=True` → Original-Exception zu GeminiError gewrappt → Task 3 Step 2; Test `test_persistent_503...` (match "API call failed") ✓
- Backoff 1s/4s/16s, max 4 Calls (Lesart A) → Task 0 (Verifikation) + Task 3 `_RETRY`; Test `...after_4_calls` ✓
- Nur 503/429, nicht 500/4xx≠429 → Tests `test_no_retry_on_server_error_500`, `test_no_retry_on_client_error_400` ✓
- Bisheriges Skip-Verhalten erhalten → bestehende 7 Tests bleiben grün (Task 3 Step 3) ✓
- tenacity direkte Dependency → Task 1 ✓
- Integrationstest gegen CachedGeminiClient → Task 4 ✓
- Token-Limit-Pfad unverändert (kein Retry) → `score_ticker` behält die `> max_input_tokens`-Logik außerhalb der dekorierten Methoden (Task 3 Step 2) ✓

**Placeholder-Scan:** Keine TBD/TODO/„später". Jeder Code-Step zeigt vollständigen Code. ✓

**Typ-Konsistenz:** `_is_retryable(exc)` / `_RETRY` / `_count_tokens(self, prompt)` / `_generate(self, prompt, max_output_tokens)` durchgängig identisch in Plan-Code und Tests. Fixture nutzt exakt `GeminiClientImpl._count_tokens.retry.sleep` / `._generate.retry.sleep` (tenacity-Decorator exponiert `.retry`). ✓
